# Where the API lives. In dev, vite proxies /api to this target.
VITE_API_TARGET=http://localhost:4000
# In production set the full API origin instead (e.g. https://api.example.com)
VITE_API_BASE_URL=
